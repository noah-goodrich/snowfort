github: [noah-goodrich]

# Suggested tiers (configured in the Sponsors UI when enabled):
#   $5/mo  — individual sponsor
#   $25/mo — small org / team sponsor
#   $500/mo — company stretch tier (logo listing in README)
#
# Sponsors is not yet enabled in repo settings. This file stages the config
# so it goes live the moment Noah clicks "Enable Sponsors" in the UI.

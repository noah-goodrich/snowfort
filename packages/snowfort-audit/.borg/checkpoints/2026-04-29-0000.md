## 1. Goal

Run `/simplify` on the `feat/directive-b-cost-038-040` branch to catch code quality, reuse, and
efficiency issues introduced by the COST_038–040 rule additions and security hardening pass.

## 2. Accomplished

Three review agents (reuse, quality, efficiency) analyzed the 74 KB branch diff. Six fixes were
applied and all 1023 unit tests continue to pass:

- **`use_cases/online_scan.py`** — Replaced hand-rolled `chr(39)` single-quote escaping with
  `escape_string_literal()` from the `sql_safety` module shipped in this same branch. The old
  form also silently missed backslash escaping.
- **`infrastructure/cortex_synthesizer.py`** — Replaced Python 2–era `counter = {"n": 0}` dict
  workaround with `nonlocal n` (idiomatic Python 3).
- **`domain/rules/sizing.py`** — Removed WHAT SQL comment before the HAVING clause in
  `ConsolidationCandidatesCheck`.
- **`domain/rules/cortex_cost.py`** — Trimmed two WHAT sentences ("costs one round-trip / implicitly
  cached by ScanContext") from the `_discover_sensitive_columns` docstring; kept the WHY (fail-closed
  behaviour).
- **`domain/rules/static.py`** — Replaced two-line WHAT comment on `_SECRET_KEYWORD_RE` with a
  single WHY line explaining the nosec suppression.
- **`tests/unit/test_no_silent_swallow.py`** — Replaced transient "PR #1 AC-1" task references in
  the module docstring and assert message with durable labels.

**Intentional non-fixes** (out of scope or false positives):
- `_restrict_to_owner` chmod pattern not abstracted (different domain from `keypair_utils`).
- `_cortex_disabled_by_env` using `os.environ` directly (DI gateway refactor is a larger change).
- `online_scan.py:362` bare FQDN f-string kept as-is (used as a dict key, not interpolated into SQL).
- `CloneSprawlCheck` no date window (adding a CREATED filter changes business logic, not simplicity).
- 90-day ACCESS_HISTORY lookback in COST_040 kept (narrowing to 30 days would incorrectly flag
  tables last accessed 31–89 days ago as stale when the output staleness threshold is 30 days).

## 3. Ready to Commit

Six files are modified and ready to commit:

```
packages/snowfort-audit/src/snowfort_audit/domain/rules/cortex_cost.py
packages/snowfort-audit/src/snowfort_audit/domain/rules/sizing.py
packages/snowfort-audit/src/snowfort_audit/domain/rules/static.py
packages/snowfort-audit/src/snowfort_audit/infrastructure/cortex_synthesizer.py
packages/snowfort-audit/src/snowfort_audit/use_cases/online_scan.py
packages/snowfort-audit/tests/unit/test_no_silent_swallow.py
```

`/simplify` has been run on all changed files this session. Ready to commit.

## 4. Blockers

No blockers.

## 5. Next Session

Create a PR for `feat/directive-b-cost-038-040` into `main`. Before opening:
1. Commit the six simplify fixes above (`chore(audit): simplify pass — COST_038–040 branch`).
2. Run `/borg-assimilate` to evaluate against `PROJECT_PLAN.md` acceptance criteria — specifically
   verify that COST_038, COST_039, and COST_040 are wired in `rule_registry.py` and the snapshot
   fixture (`tests/unit/fixtures/rules_snapshot.yaml`) is updated.
3. Run `gh pr create` targeting `main`.

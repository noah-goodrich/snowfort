## 1. Goal
Implement Directive E (IaC Drift Detection + dbt Grant Analysis) for snowfort-audit: 4 new rules
(OPS_015, OPS_016, GOV_025, GOV_026) with conventions, shared helpers, unit tests, and live validation.

## 2. Accomplished
- **`src/snowfort_audit/domain/conventions.py`** — Added `IacDriftThresholds` and `DbtGrantsThresholds`
  frozen dataclasses, wired into `RuleThresholdConventions` as `iac_drift` and `dbt_grants` fields
- **`src/snowfort_audit/domain/rules/_iac.py`** — Created new shared helper module: 3 QUERY_HISTORY
  fetchers (`qh_iac_fetcher`, `qh_ddl_non_svc_fetcher`, `qh_grant_fetcher`), `schema_owners_fetcher`,
  `detect_iac_tools()`, `managed_tag_coverage_by_database()`, `parse_grant_target_role()`, and
  `_sf_regex()`/`_sf_regexp_like()` Snowflake regex adapter
- **`src/snowfort_audit/domain/rules/iac_drift.py`** — 4 rule classes: `IacToolDetectionCheck` (OPS_015),
  `IacDriftIndicatorsCheck` (OPS_016), `DbtGrantTargetValidationCheck` (GOV_025),
  `DbtSchemaOwnershipCheck` (GOV_026)
- **`src/snowfort_audit/domain/rules/__init__.py`** — Added imports and `__all__` entries for all 4 new rules
- **`src/snowfort_audit/infrastructure/rule_registry.py`** — Registered 4 rules under Directive E section
  with conventions injection
- **`tests/unit/test_iac_drift_rules.py`** — 50 unit tests covering all 4 rules + helper functions
  (all passing)
- **`tests/unit/fixtures/rules_snapshot.yaml`** — Regenerated with OPS_015, OPS_016, GOV_025, GOV_026
- **`docs/plans/directives/PROJECT_PLAN.md`** — Written with objectives, acceptance criteria,
  Collective Review summary
- **Bug fix**: Snowflake `REGEXP_LIKE` doesn't support Python `(?i)` inline flags — built
  `_sf_regexp_like()` adapter that strips `(?i)` and passes `'i'` as the third parameter
- **Full gate**: ruff + pylint + import-linter + mypy all clean; 1278 tests passed, 4 skipped
- **Live validation**: All 4 rules executed against `dyb56910` account — OPS_015 correctly detected
  "No IaC tools" as INFORMATIONAL; OPS_016/GOV_025/GOV_026 returned 0 findings (correct for account
  without IaC tooling)

## 3. Ready to Commit
All files below are changed and ready. `/simplify` has **not** been run this session — recommend doing
so before committing:

- `src/snowfort_audit/domain/conventions.py`
- `src/snowfort_audit/domain/rules/_iac.py` (new)
- `src/snowfort_audit/domain/rules/iac_drift.py` (new)
- `src/snowfort_audit/domain/rules/__init__.py`
- `src/snowfort_audit/infrastructure/rule_registry.py`
- `tests/unit/test_iac_drift_rules.py` (new)
- `tests/unit/fixtures/rules_snapshot.yaml`
- `docs/plans/directives/PROJECT_PLAN.md` (new)

## 4. Blockers
No blockers. All acceptance criteria from PROJECT_PLAN.md are met.

## 5. Next Session
1. **Run `/simplify` on the 8 changed files**, then commit Directive E.
2. **Run `borg-assimilate`** to create the assimilated plan at
   `docs/plans/assimilated/2026-05-01-directive-e-iac-drift-dbt-grants.md`.
3. **Identify the next directive** — check `docs/plans/directives/` for remaining unassimilated
   directives (Interactive Remediation Skill is next but is pure skill/agent work, not a code
   directive; look for the next code-level directive).
4. **Key learning to carry forward**: Snowflake `REGEXP_LIKE` does not support Python-style `(?i)`
   inline flags. Always use `_sf_regexp_like()` from `_iac.py` (or the same pattern) when building
   SQL with user-configurable regex patterns. The `make check` target requires `pytest` binary on
   PATH — use `python3 -m pytest` as a workaround on this system.

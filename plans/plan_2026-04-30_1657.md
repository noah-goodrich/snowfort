---
created: 2026-04-30T16:57:45.317Z
session: 3dfa0043-9586-4f27-b97a-cfb9c879a630
working_directory: /Users/noah/dev/snowfort
---

## Plan: Directive F — Cortex AI Governance

### Pre-work: Fix PR 16 CI
- `rbac.py:433` — append `# sensitive-output-ok: describes auth concept, not a credential` to the line containing "password access"
- Amend commit, force-push → CI re-runs → merge

---

### Step 1 — Extend `CortexThresholds` in `conventions.py`
Add 8 fields to the frozen dataclass:
```python
power_user_concentration_threshold: float = 0.80
power_user_min_users: int = 3
growth_rate_threshold: float = 0.50
growth_consecutive_months: int = 2
fine_tuning_unused_days: int = 30
agent_max_daily_sessions: int = 1000
search_corpus_size_threshold_gb: int = 10
function_sprawl_threshold: int = 5
```
Add a test to `test_conventions_thresholds.py` covering the 8 new defaults.

---

### Step 2 — COST_041–044 in `cortex_cost.py`
Add 4 new `_CortexRule` subclasses before `get_cortex_rules()`, then add them to the factory under `# D8 — Directive F cost rules`:

| Class | Rule ID | View | Key logic |
|---|---|---|---|
| `CortexAutoModelRoutingCheck` | COST_041 | `CORTEX_AI_FUNCTIONS_USAGE_HISTORY` | Detect explicit large-model calls; INFORMATIONAL |
| `CortexPowerUserConcentrationCheck` | COST_042 | `CORTEX_AI_FUNCTIONS_USAGE_HISTORY` | Top-user credit share > 80% of <3 users; MEDIUM |
| `CortexPublicAccessCheck` | COST_043 | Direct cursor: `SHOW GRANTS ON FUNCTION SNOWFLAKE.CORTEX.COMPLETE(...)` + other Cortex functions | Flag PUBLIC grantee; HIGH |
| `CortexSessionGrowthTrendCheck` | COST_044 | `METERING_DAILY_HISTORY` | Month-over-month growth >50% for 2+ consecutive months; LOW |

COST_043 uses direct cursor (not `_CortexRule` cache) since `SHOW GRANTS` returns different row shapes than usage history views.

---

### Step 3 — New `cortex_governance.py` (CORTEX_001–007)
New file under `domain/rules/`. Subclasses `_CortexRule` where using cached views, direct cursor elsewhere:

| Class | Rule ID | Data source | Severity |
|---|---|---|---|
| `CortexSearchServiceGovernanceCheck` | CORTEX_001 | `SHOW CORTEX SEARCH SERVICES` + grants | MEDIUM |
| `CortexAnalystSemanticModelAuditCheck` | CORTEX_002 | `QUERY_HISTORY` (Analyst calls) | MEDIUM |
| `CortexAgentGovernanceCheck` | CORTEX_003 | `QUERY_HISTORY` (agent calls) + grants | HIGH |
| `CortexIntelligenceGovernanceCheck` | CORTEX_004 | `METERING_DAILY_HISTORY` + grants | MEDIUM |
| `CortexFineTuningCostTrackingCheck` | CORTEX_005 | `QUERY_HISTORY` (fine-tune + inference) | MEDIUM |
| `CortexLLMFunctionSprawlCheck` | CORTEX_006 | `QUERY_HISTORY` (distinct function types) | LOW |
| `CortexServerlessAIBudgetGapCheck` | CORTEX_007 | `METERING_DAILY_HISTORY` + `BUDGETS` view | HIGH |

All rules: `is_allowlisted_sf_error()` guards, `RuleExecutionError` re-raise, no bare swallows.
Module exposes `get_cortex_governance_rules(conventions, telemetry)` factory.

---

### Step 4 — Wire into registry + `__init__.py`
- `rule_registry.py`: add `from snowfort_audit.domain.rules.cortex_governance import get_cortex_governance_rules` and `*get_cortex_governance_rules(...)` splat in `get_all_rules()`
- `domain/rules/__init__.py`: no individual exports needed (governance rules follow the `cortex_cost` factory pattern, not the RBAC per-class pattern)

---

### Step 5 — Tests
- **`test_cortex_cost_rules.py`**: Add test classes for COST_041–044 (~5 tests each = ~20 tests)
  - Standard 4: `test_none_scan_context`, `test_view_unavailable`, `test_over_threshold`, `test_under_threshold`
  - COST_041: add `test_auto_model_no_violation`, `test_explicit_large_model_flags`
  - COST_042: add `test_even_distribution_no_violation`, `test_single_user_dominates_flags`
- **`tests/unit/test_cortex_governance_rules.py`**: New file, ~5 tests per rule = ~35 tests
  - Same mock helpers as cortex_cost tests
  - CORTEX_003: add `test_external_tool_access_flags`, `test_public_access_flags`
- Update `test_no_silent_swallow.py` scan paths to include `cortex_governance.py`

---

### Step 6 — Snapshot regen + final verification
```bash
UPDATE_SNAPSHOT=1 pytest tests/unit/test_rules_snapshot.py
grep "COST_04[1-4]\|CORTEX_00[1-7]" tests/unit/fixtures/rules_snapshot.yaml | wc -l  # → 11
cd packages/snowfort-audit && PATH=".venv/bin:$PATH" make check  # green
python scripts/check_sensitive_outputs.py  # no findings
```

---

### Step 7 — Ship
Commit, PR, write assimilated plan to `docs/plans/assimilated/2026-04-30-directive-f-cortex-ai-governance.md`, merge.

---

### Acceptance Criteria

1. `make check` green — lint, mypy, ≥80% coverage
   - `cd packages/snowfort-audit && PATH=".venv/bin:$PATH" make check`
2. All 11 rule IDs in snapshot
   - `grep "COST_04[1-4]\|CORTEX_00[1-7]" tests/unit/fixtures/rules_snapshot.yaml | wc -l` → 11
3. 8 new threshold fields in `CortexThresholds` defaults
   - `python -c "from snowfort_audit.domain.conventions import CortexThresholds; t = CortexThresholds(); print(t.power_user_concentration_threshold, t.function_sprawl_threshold)"` → 0.8 5
4. Sensitive-outputs lint passes
   - `python scripts/check_sensitive_outputs.py` → exit 0
5. Each new rule returns `[]` when `scan_context=None`
   - Covered by `test_none_scan_context_returns_empty` per rule

### Not building
- Cross-account Cortex federation
- Automated remediation (disabling functions, revoking PUBLIC)
- `SYSTEM$CLASSIFY`-based detection
- Directive E (IaC drift) — separate directive

### Files changed
| File | Change |
|---|---|
| `domain/conventions.py` | +8 fields to `CortexThresholds` |
| `domain/rules/cortex_cost.py` | +4 rule classes + factory entries |
| `domain/rules/cortex_governance.py` | **New** — 7 rule classes + factory |
| `infrastructure/rule_registry.py` | +`get_cortex_governance_rules()` registration |
| `tests/unit/test_cortex_cost_rules.py` | +~20 tests for COST_041–044 |
| `tests/unit/test_cortex_governance_rules.py` | **New** — ~35 tests |
| `tests/unit/fixtures/rules_snapshot.yaml` | Regenerated |
| `docs/directives/PROJECT_PLAN.md` | Replaced with Directive F plan |
| `rbac.py:433` | `# sensitive-output-ok` annotation |

---

## User Context

make sure to write the project_plan.md first.